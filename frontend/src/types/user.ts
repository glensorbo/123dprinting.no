export type User = {
  _id: string;
  name: string;
  email: string;
  // access: Types.ObjectId[];
  // company?: Types.ObjectId;
  // team?: Types.ObjectId;
  // position?: Types.ObjectId;
  // invitation?: Types.ObjectId;
  status: string;
  active: boolean;
};
